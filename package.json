{
  "name": "cw-auto-play",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
  "test": "npx playwright test",
  "test:with-report": "npx playwright test",
  "test:clean": "if exist allure-results rmdir /s /q allure-results && npx playwright test",
  "report:playwright": "node scripts/forceShowReport.js",
  "report:allure": "node scripts/forceShowAllure.js",
  "test:full": "rmdir /s /q allure-results && npx playwright test && node scripts/forceShowReport.js && allure generate allure-results --clean -o allure-report && node scripts/forceShowAllure.js"
},



  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.3",
    "allure-playwright": "^3.2.2",
    "kill-port": "^2.0.1"
  },
  "dependencies": {
    "better-sqlite3": "^12.1.1",
    "dotenv": "^16.5.0"
  }
}
